# Comodo

## Cloudflare Pages quick deploy

This repo now includes `index.html` so Cloudflare Pages serves a valid homepage instead of a 404.

- Build command: *(leave empty)*
- Build output directory: `/` (root)

> Note: Full backend features (`/ask`, file APIs, project memory) run in `codeeditorcode.py` (Flask). If you deploy only static Pages, those backend routes are not available unless you add a server/worker API.
